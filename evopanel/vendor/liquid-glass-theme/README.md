# Liquid glass theme (vendored build input)

This directory holds **source fragments** used by
`evopanel/scripts/build-glass-shader.mjs` to generate
`evopanel/src/lib/liquid-glass/glass-shader.js`.

It is **not** a DeepSeek / DSH product package. Runtime UI loads QAgent’s own
`src/lib/liquid-glass/*` modules only.

## Upstream

Adapted from the MIT-licensed project:

https://github.com/Rainpomelo/deepseek-harness-liquid-glass-theme

See `LICENSE` in this folder and the repository root `NOTICE`.
