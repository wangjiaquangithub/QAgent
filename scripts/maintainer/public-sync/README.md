# Public sync helpers

Scripts used by `.github/workflows/sync-public.yml` and `scripts/maintainer/windows/local-publish.ps1`.
Formerly lived under `website/scripts/` before the website was extracted to Quclouds/evoflow-website.
